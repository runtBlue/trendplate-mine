(function() {
  var ready;

  ready = function() {
    var scope;
    scope = document.getElementById('page--index');
    if (!scope) {
      return;
    }
  };

  document.addEventListener('DOMContentLoaded', ready, false);

}).call(this);
